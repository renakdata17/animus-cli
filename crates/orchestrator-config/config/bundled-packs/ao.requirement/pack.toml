schema = "ao.pack.v1"
id = "ao.requirement"
version = "0.1.0"
kind = "domain-pack"
title = "AO Requirement"
description = "Bundled first-party requirement planning and materialization workflows."

[ownership]
mode = "bundled"

[compatibility]
ao_core = ">=0.1.0"
workflow_schema = "v2"
subject_schema = "v2"

[subjects]
kinds = ["ao.requirement"]
default_kind = "ao.requirement"

[workflows]
root = "workflows"
exports = [
  "ao.requirement/draft",
  "ao.requirement/refine",
  "ao.requirement/plan",
  "ao.requirement/execute",
]

[[dependencies]]
id = "ao.task"
version = ">=0.1.0"
reason = "Requirement workflows materialize and bootstrap task execution."

[runtime]
agent_overlay = "runtime/agent-runtime.overlay.yaml"

[permissions]
tools = ["ao", "Read", "Glob", "Grep", "Bash"]

[native_module]
feature = "plugin_ao_requirement"
module_id = "ao.requirement"
optional = true
